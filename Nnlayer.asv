function [obj]=Nnlayer(N,Xn,Mlinlnm)
    obj.N = N;
    obj.Mlinlnm = Mlinlnm;
    obj.Xn = Xn;
    
    for i=1:N
        obj.input(i,:) =  obj.Xn(i)*obj.Mlinlnm(i,:);
        obj.Nneuron{i} = @Nneuron;
    end
    for i=1:obj.N
       for j=1:obj.N
            obj.Nneuron{i}(obj.input(i),obj.input);
       end
       obj.output(i) =
    end
end

